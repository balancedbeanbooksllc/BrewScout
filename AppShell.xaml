<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:BrewScout.Views"
    x:Class="BrewScout.AppShell">
  <TabBar>
    <ShellContent Title="Breweries" ContentTemplate="{DataTemplate views:BreweryListPage}" />
    <ShellContent Title="Notes" ContentTemplate="{DataTemplate views:TastingNotesPage}" />
    <ShellContent Title="Events" ContentTemplate="{DataTemplate views:EventsListPage}" />
    <ShellContent Title="Map" ContentTemplate="{DataTemplate views:MapPage}" />
  </TabBar>
</Shell>
